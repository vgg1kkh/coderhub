const connections = require("../app/database")

class authService {
    checkMoment(user_id,moment_id){
        console.log("in checkMoment, userId, momentId")
        const statement = ``
        return true
    }
}

module.exports= new authService()