const connections = require("../app/database")

class authService {
    checkMoment(user_id,moment_id){
        console.log("in checkMoment, userId, momentId",user_id,momn)
        const statement = ``
        return true
    }
}

module.exports= new authService()